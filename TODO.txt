- different score table for each difficulty / lives?
- to save memory (CD32 version?): don't include white arremers, shorten sprite files, lower quality sound samples
  => arremer table index=>is_arremer to fix clut to red else it will become invisible!
- if same level context as before, it seems that the game still loads the data despite the attempt
  to optimize (data is already in memory), seen on CD32 version  

- after continuing, the map context should be loaded!! sometimes ok sometimes not (release mode level 5??). Maybe another clear screen
  race condition? maybe fixed now
- After entering a highscore and being too fast on the continues screen with pressing fire to continue, the game switches to the map (as expected), but it's playing the wrong music: it's repeating the last part from the highscore screen instead of the "map music" and you'll start the level without any music at all. Once you finish the level (or in my case more likely: die), all goes back to normal.
- another curious thing about the time (playing normally this one), when the two minutes counter ends, a :32 is displayed when you are dying, until the next life begins 
- 50Hz flashy at times, limit framerate?




- pre-mirror of trunk fails (91) in clut 1 probably because of 0 in planelist (ok on non-blit chain, blit chain specifics, same issue
  when using fixed value for blitter data instead of source, annoying!)
- blit chain: scrolling to fix, wrong values, shaking
- remove parasite tiles (in the end) because of levelskip some intro tiles are logged in levels 2+
- remove those "hhhhh" at start fill_screen_with_h 
- level 3 still tile issues (part 2, hard left after big ladder, even on emulation, try to reproduce as not systematic, maybe release only)
- real hardware sometimes scrolling seems to reset. Increase delta x scroll to trigger full refresh: to test real hw
- try to apply patches to fix some original game bugs / difficulty level HP for monsters
- add foreground tiles (grass, bridge, level 6 pillars): tricky work, added blitting, and not really noticeable
- demo resets scroll and makes column update buggy at times
- level 2 see if we can use dynamic color change, see if separating ice towers from rest of game tiles (except house right after)
  allows a neat 32+32 colors. In that case, it's doable to use dynamic color change on some colors
- diff with world version 3 see if the fixes are visible: difficult. 1 diff seen around DDFD, rom bank $4000 to check
- force tile draw cheat screws up host_scroll and tile feed
- change weapon trainer: call the routine to redraw weapon: not from interrupt!! if must be done, force to call "a new
  weapon is picked up" with a flag


hacks:

block constant palette refresh: bpset 65fb,,{PC=6655;g}
same background tiles when inserting coin: bpset 6b99,,{B=1;g} (B:0-3)
first color of sprites purple:
fill $3841,1,$F0
fill $3941,1,$F0





replace division: 

bpset FED8,,{temp0=A;A /= B; B = A % B;PC=FEEF;g}
bpset FEF0,,{temp0=w@U; temp1=temp0/D; w@U=temp1; D = temp0%D; PC=$feef; g}

fix random (warning: also random with timer @ 21)

bpset 68f9,,{B=0;PC=6908;g}

log a value to screen:

wpset D04,1,W,,{b@20B0=wpdata;g}


log sounds: bpset 7942,,{print B;g}
skip sounds: bpset 7942,B == $17,{PC=$792F;g}
record sounds: bpset 791D,B != $FF,{B=$E;g}
or simply service mode!!

log dyn colors:
wpset 1632,40,W,,{printf "%02X:%03X",wpaddr-$1632,((wpdata<<8)+(b@(wpaddr+$40)))>>4;g}

remove music:

bpset 7A24,,{PC=7945;g}
bpset 7A39,,{PC=7945;g}
bpset 794B,,{PC=7945;g}
bpset 7A4D,,{PC=7945;g}
bpset 8FCE,,{PC=8FD1;g}

longplay: https://www.youtube.com/watch?v=SaqtjNWOESs&t=2557s
japanese longplay: https://youtu.be/DwemSrKpchA
vgms: https://vgmrips.net/packs/pack/ghosts-n-goblins-arcade
strategy: https://strategywiki.org/wiki/Ghosts_%27n_Goblins/Stage_1