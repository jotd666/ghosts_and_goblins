- big issue (cycle exact+release) completely trashed at level start (random, any level)
  could it be the removal of sprite clipping? tile mirror?
- water animation: red in it!
- when loading level 1 context, also load level12.mod
- level 3 still tile issues (part 2, hard left after big ladder, only seen on real hw): to test real hw
- real hardware sometimes scrolling seems to reset. Increase delta x scroll to trigger full refresh: to test real hw
- continue play: part of the map is black: not always???
- highscore reached: doesn't load level 1 music during attract mode, stuck on highscore .mod
- load/save highscores
- CD32 version to test with release exe: ATM too much mem needed. but not including 400k music buffer will help!
  => need to record the tunes with several loops using service mode and 3F to stop sound: finish wav splitting
- option to start without music, in which case all sounds except a given list should use automatic channel
  => use that in CD32
- implement dip difficulty
- fg layer: lives colors are not good. Black!=transparent, maybe rework or rip with changed
  0x4B4 and next colors: try to get sheet D correct. 0x9A life tile. Why frame has black ?? should be transparent
  palette to separate black from background: make it magenta on all sheets to extract black properly.
  maybe just brute convert black to magenta, then adjust the frame & lives icon, the others don't need black
- check if game tries to load music from level 1 to 2 or if it's cached
- bottom left life icon not erased by "credit" message
- start at level 5: tries to load level 2 then loads level 5
- elaborate sound priorities
- add foreground tiles (grass, bridge)

- 50Hz to do with blitter chaining: risk of flickering because we can't store mirrored copies
- scrolling and double buffering can it be improved? (also in other games!)
- diff with world version 3 see if the fixes are visible: difficult. 1 diff seen around DDFD, rom bank $4000 to check
- try to apply patches to fix some original game bugs / difficulty level HP for monsters
- 6ED0 called at the start of a level, maybe try to load context there first
- force tile draw cheat screws up host_scroll and tile feed
- remove parasite tiles (in the end) because of levelskip some intro tiles are logged in levels 2+
- inprint the tombstones (level 1 & 3) in the background (for instance when drawing left & right tiles but also at start) to save
  cpu & reduce the shaking effect
- change weapon trainer: call the routine to redraw weapon: not from interrupt!! if must be done, force to call "a new
  weapon is picked up" with a flag
- demo: scroll resets but probably min/max scroll not recomputed properly which leads to visible scroll update: maybe solved


- 0x38, # last boss music (resolve your battle)
- 0x2A, # boss music (in game)
- 0x39, # unused jingle
- 0x29, # castle music (lev 5)
- 0x11: unused sfx (it seems)
- 0x32: win

hacks:

block constant palette refresh: bpset 65fb,,{PC=6655;g}
same background tiles when inserting coin: bpset 6b99,,{B=1;g} (B:0-3)
first color of sprites purple:
fill $3841,1,$F0
fill $3941,1,$F0





replace division: 

bpset FED8,,{temp0=A;A /= B; B = A % B;PC=FEEF;g}
bpset FEF0,,{temp0=w@U; temp1=temp0/D; w@U=temp1; D = temp0%D; PC=$feef; g}

log sounds: bpset 7942,,{print B;g}
skip sounds: bpset 7942,B == $17,{PC=$792F;g}
record sounds: bpset 791D,B != $FF,{B=$E;g}
or simply service mode!!

log dyn colors:
wpset 1632,40,W,,{printf "%02X:%03X",wpaddr-$1632,((wpdata<<8)+(b@(wpaddr+$40)))>>4;g}

remove music:

bpset 7A24,,{PC=7945;g}
bpset 7A39,,{PC=7945;g}
bpset 794B,,{PC=7945;g}
bpset 7A4D,,{PC=7945;g}
bpset 8FCE,,{PC=8FD1;g}

longplay: https://www.youtube.com/watch?v=SaqtjNWOESs&t=2557s
vgms: https://vgmrips.net/packs/pack/ghosts-n-goblins-arcade
