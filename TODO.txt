- scrolling in HW test (with level 3 tiles): right ok to a point, left: make work when scroll=0x100
  it fails but ok otherwise: check boundary D0 offset between ok and wrong (like I did with right scroll)
- refactor to get draw address first, to remove extra 16 pixels on the top (corkscrew)
- add space before and after
- Y scroll: first perform wrap at 256+16 (copper split)
- check if palette different on map, then log tiles (and load)
- grouping, seems to have issues with main character at tile 0 tile 0 seems
  to be a special case despite the precautions
- detect intro fade palette
- try to detect level 2, palette is not exactly the same as level1
  some colors are not in the same order
- debug division routines: plant shoot angles are wrong, devils attacks maybe (level 3 first devil debug)
- tombstone easter egg shot seems wrong
- parasite sprites at the bottom
- flag system to avoid re-copying tiles again and again if already correct
- log tiles (per level): with zone filtering (first level only upper part)
- use 16-bit table for mirror
- flame in HUD wrong colors
- fg tiles color reduction is too violent, maybe do a manual reduction
  or dynamic color change between scores and in-game, try to keep the
  maximum of different colors on the bottom status
- update bg tiles do that using dirty not by brutal refresh as it's done twice (color+tilecode)
- fg layer: isolate frame+weapon+lives (everything non-letter on the bottom), use dynamic palette
- cpu log: control should be able to log with SR=x3xx or SR=x0xx
  here we want to avoid logging within the interrupt


hacks:

block constant palette refresh: bpset 65fb,,{PC=6655;g}
same background tiles when inserting coin: bpset 6b99,,{B=1;g} (B:0-3)
first color of sprites purple:
fill $3841,1,$F0
fill $3941,1,$F0


log sounds: bpset 7942,,{print B;g}
skip sounds: bpset 7942,B==$17,{PC=$792F;g}

remove music:

bpset 7A24,,{PC=7945;g}
bpset 7A39,,{PC=7945;g}
bpset 794B,,{PC=7945;g}
bpset 7A4D,,{PC=7945;g}
bpset 8FCE,,{PC=8FD1;g}