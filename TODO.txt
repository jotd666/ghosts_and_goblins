- Unmapped sound indexes: ['0x11', '0x13', '0x15', '0x32', '0x39']
- fix some sfx on channel 3, elaborate sound priorities, maybe remove goblin sounds as they cut off the music too much
- log sprites level 7? pb with existing pairs WTF
- no music on level 7, but then no music at all
- sprites clipped too low (devil on level 5, player level 2 on towers?)
- map: sometimes map context not detected, which leads to no map or corrupt colors map (depending on the levels)
- dynamic palette in water level 1 intercept it and simulate it, level 4 has this too (flames): change_a_few_colors_ee44
- infinite lives trainer not working

- CD32 version to test with release exe
- insert coin with fire/start with up/down
- use 16-bit table for mirror but not mirror routine as it's in place
- start at level 5: tries to load level 2 then loads level 5
- fg layer: lives colors are not good. Black!=transparent, maybe rework or rip with changed
  palette to separate black from background- need to implement/test 64 bit swap (last boss)
- depending on the level, do not mirror Y (intro, level 1, level 4, level 7 don't need it)
- check if game tries to load music from level 1 to 2 or if it's cached
- tombstone easter egg shot seems wrong
- bottom left life icon not erased by "credit" message
- log tiles (per level): with zone filtering (first level only upper part)
- map: sceptre of player rendered on top too sometimes: still ??? to test

- option to start without music, in which case all sounds except a given list should use automatic channel
- 6ED0 called at the start of a level, maybe try to load context there first
- force tile draw cheat screws up host_scroll and tile feed
- remove parasite tiles (in the end) because of levelskip some intro tiles are logged in levels 2+
- inprint the tombstones (level 1 & 3) in the background (for instance when drawing left & right tiles but also at start) to save
  cpu & reduce the shaking effect
- change weapon trainer: call the routine to redraw weapon: not from interrupt!! if must be done, force to call "a new
  weapon is picked up" with a flag


- 0x38, # last boss music (resolve your battle)
- 0x2A, # boss music (in game)

- 0x29, # castle music (lev 5)

hacks:

block constant palette refresh: bpset 65fb,,{PC=6655;g}
same background tiles when inserting coin: bpset 6b99,,{B=1;g} (B:0-3)
first color of sprites purple:
fill $3841,1,$F0
fill $3941,1,$F0


replace division: 

bpset FED8,,{temp0=A;A /= B; B = A % B;PC=FEEF;g}
bpset FEF0,,{temp0=w@U; temp1=temp0/D; w@U=temp1; D = temp0%D; PC=$feef; g}

log sounds: bpset 7942,,{print B;g}
skip sounds: bpset 7942,B == $17,{PC=$792F;g}
record sounds: bpset 791D,B != $FF,{B=$E;g}


remove music:

bpset 7A24,,{PC=7945;g}
bpset 7A39,,{PC=7945;g}
bpset 794B,,{PC=7945;g}
bpset 7A4D,,{PC=7945;g}
bpset 8FCE,,{PC=8FD1;g}