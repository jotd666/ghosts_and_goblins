- devil bug: would need to protect all jump tables:
7413: CE 74 18    LDU    #jump_table_7418
7416: 6E D5       JMP    [B,U]        ; [indirect_jump]
more common case so get table, count the numbers of entries and change [indirect_jump] => [indirect_jump] (x cases) then change postproc
to add arg in jxx macro

- right now state of devil is altered because EB5 takes value 2 which is out of range for jump table at BBA5
- when writing an isolated tile (video_byte_dirty) check if in screen and find proper offset (not working right now
  picks a tile at wrong location, also when forcing screen display misses totally if scroll=0x100 - level 6)
  also when resuming level 2 it displays black screen too
  probably responsible for buggy tiles when scrolling up
- level 6: no tiles display at all!
- start at level 5: tries to load level 2 then loads level 5
- missing water tile from intro / level 2
- empty/hole tile on the top (cave) when scrolling
- cave: black row appearing up when standing on tomb.
- sprites disappear too early on the left
- need to implement/test 48 and 64 bit swap (last boss)
- depending on the level, do not mirror Y (level 1, level 4 don't need it)
- grouping, seems to have issues with main character at tile 0 tile 0 seems
  to be a special case despite the precautions, use mockup to see what's happening
- game over: game continues!
- when starting the game, we should stop the music as it makes the game crash: TBC
- map: sceptre of player rendered on top too
- doesn't seem to detect map context change at times (tiles OK but palette is broken)
- level 1: first devil stays forever sometimes (unkillable) if we approach too closely without killing him
  no more enemies in level then same devil strange issue on level 3
- dynamic palette in water level 1 intercept it and simulate it
- remove parasite tiles (in the end) because of levelskip some intro tiles are logged in levels 2+
  
- inprint the tombstones (level 1 & 3) in the background (for instance when drawing left & right tiles but also at start) to save
  cpu & reduce the shaking effect
- fix some sfx on channel 3, elaborate sound priorities
- infinite lives trainer not working
- restore_background: should restore 5 first bitplanes, then clear the last one
- check if game tries to load music from level 1 to 2 or if it's cached
- tombstone easter egg shot seems wrong
- bottom left life icon not erased by "credit" message
- log tiles (per level): with zone filtering (first level only upper part)
- use 16-bit table for mirror
- fg layer: lives colors are not good. Black!=transparent, maybe rework or rip with changed
  palette to separate black from background
- option to start without music, in which case all sounds except a given list should use automatic channel


- 0x38, # last boss music (resolve your battle)
- 0x2A, # boss music (in game)

- 0x29, # castle music (lev 5)

hacks:

block constant palette refresh: bpset 65fb,,{PC=6655;g}
same background tiles when inserting coin: bpset 6b99,,{B=1;g} (B:0-3)
first color of sprites purple:
fill $3841,1,$F0
fill $3941,1,$F0


replace division: 

bpset FED8,,{temp0=A;A /= B; B = A % B;PC=FEEF;g}
bpset FEF0,,{temp0=w@U; temp1=temp0/D; w@U=temp1; D = temp0%D; PC=$feef; g}

log sounds: bpset 7942,,{print B;g}
skip sounds: bpset 7942,B == $17,{PC=$792F;g}
record sounds: bpset 791D,B != $FF,{B=$E;g}


remove music:

bpset 7A24,,{PC=7945;g}
bpset 7A39,,{PC=7945;g}
bpset 794B,,{PC=7945;g}
bpset 7A4D,,{PC=7945;g}
bpset 8FCE,,{PC=8FD1;g}