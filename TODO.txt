- background tiles are bogus when game starts
- sprite coords shifted to the left
- enable symmetry
- erase sprite to get proper intro sequence
- flag system to avoid re-copying tiles again and again if already correct
- why purple flashes? all chars are logged properly
- log tiles (per level): with zone filtering (first level only upper part)
- division by zero at some point, see what it does on real code FED8
- contexts using tile palette values or current level values (tile colors?)
  switch tile palette / tile set when palette matches the level
  caution: intro screen map has its own palette too
- use 16-bit table for mirror
- fg tiles color reduction is too violent, maybe do a manual reduction
  or dynamic color change between scores and in-game, try to keep the
  maximum of different colors on the bottom status
- update bg tiles do that using dirty not by brutal refresh as it's done twice (color+tilecode)
- fg layer: isolate frame+weapon+lives (everything non-letter on the bottom), use dynamic palette
- level tiles & palette should be in a separate file, loadable through whdload or floppy trackloader (use RN?)
- cpu log: control should be able to log with SR=x3xx or SR=x0xx
  here we want to avoid logging within the interrupt


hacks:

block constant palette refresh: bpset 65fb,,{PC=6655;g}
same background tiles when inserting coin: bpset 6b99,,{B=1;g} (B:0-3)
first color of sprites purple:
fill $3841,1,$F0
fill $3941,1,$F0


