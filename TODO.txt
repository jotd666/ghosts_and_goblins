- resume from scroll X != 0 doesn't work properly, test on hw tester
- Y scroll: first perform wrap at 256+16 (copper split)
- 0x100 x shift on sprites not supported
- update_bg_tile: store draw address first and act on first column tile on the right draw, to remove extra 16 pixels on the top (corkscrew)
- level 7 crashes when killed
- game over: game locks up
- parasite sprites at the bottom
- hw_test: scrolling X: ok but tiles on the left refreshed too late: see the address
  draw_left uses

- "map" music plays but probably cut by level music at start. 3F stops music?
- level 1: first devil stays forever sometimes (unkillable), no more enemies in level then
  same devil strange issue on level 3
- dynamic palette change on level 2: check 65D1 break or hack something (when houses appear)
  we'd have to remove browns for ice palace first, then restore them
  check if water tiles aren't using the same blues
- 3F stop music should be implemented (demo => start game music remains)
- frog colors: change dynamically (detect in sprites palette) like the original
  but as the colors were quantized, make a list of the used colors of quantized
  frog frames, then try to find the best greens (the ones from the original palette change)
- add space (rows) before and after screens
- grouping, seems to have issues with main character at tile 0 tile 0 seems
  to be a special case despite the precautions, use mockup to see what's happening
- Y=0 is address 2801
- fix some sfx on channel 3, elaborate sound priorities

- restore_background: should restore 5 first bitplanes, then clear the last one
- check if game tries to load music from level 1 to 2 or if it's cached
- tombstone easter egg shot seems wrong
- bottom left life icon not erased by "credit" message
- log tiles (per level): with zone filtering (first level only upper part)
- use 16-bit table for mirror
- fg layer: lives colors are not good. Black!=transparent, maybe rework or rip with changed
  palette to separate black from background
- cpu log: control should be able to log with SR=x3xx or SR=x0xx
  here we want to avoid logging within the interrupt

- 0x38, # last boss music (resolve your battle)
- 0x2A, # boss music (in game)
- 0x33, # level 3 cave music
- 0x34, # level 3 boss dragon music
- 0x29, # castle music (lev 5)

hacks:

block constant palette refresh: bpset 65fb,,{PC=6655;g}
same background tiles when inserting coin: bpset 6b99,,{B=1;g} (B:0-3)
first color of sprites purple:
fill $3841,1,$F0
fill $3941,1,$F0


replace division: 

bpset FED8,,{temp0=A;A /= B; B = A % B;PC=FEEF;g}
bpset FEF0,,{temp0=w@U; temp1=temp0/D; w@U=temp1; D = temp0%D; PC=$feef; g}

log sounds: bpset 7942,,{print B;g}
skip sounds: bpset 7942,B == $17,{PC=$792F;g}
record sounds: bpset 791D,B != $FF,{B=$E;g}


remove music:

bpset 7A24,,{PC=7945;g}
bpset 7A39,,{PC=7945;g}
bpset 794B,,{PC=7945;g}
bpset 7A4D,,{PC=7945;g}
bpset 8FCE,,{PC=8FD1;g}